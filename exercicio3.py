# desenvolva um programa python que converta a temeperatura de celsius para firehard
print("lembre-se de usar '.' no lugar da ','!")
var_celsius = float(input('digite temperatura em ºC: '))
# executa a fórmula a armazenar em uma variável
var_fahrenheint = var_celsius * 9 / 5 + 32
 #exibe no terminal com duas casas decimais 
print(f"A temperatura é {var_fahrenheit:.2f} °f")
